export interface IProps {
  title: string
  firstRates: { active: boolean; val: string | null }
  secondRates: { active: boolean; val: string | null }
  thirdRates: { active: boolean; val: string | null }
}
